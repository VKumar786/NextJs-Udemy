/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.etimg.com",
      "assets.vogue.com",
      "m.media-amazon.com",
      "cdn.dummyjson.com",
      "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;
